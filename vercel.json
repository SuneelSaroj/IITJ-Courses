{
    "builds": [{
        "src": "courses/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11.3" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "courses/wsgi.py"
        }
    ]
}